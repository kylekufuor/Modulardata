# =============================================================================
# ModularData API - Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to Git - it contains secrets!
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Get these from your Supabase project dashboard:
# Project Settings > API > Project URL and API Keys

# The URL of your Supabase project (e.g., https://xxxxx.supabase.co)
SUPABASE_URL=https://your-project.supabase.co

# The anon/public key - safe for client-side, respects RLS policies
SUPABASE_KEY=your-supabase-anon-key

# The service_role key - bypasses RLS, use only on server-side
# DANGER: Never expose this in client-side code or logs
SUPABASE_SERVICE_KEY=your-supabase-service-key

# -----------------------------------------------------------------------------
# Redis Configuration (for Celery task queue)
# -----------------------------------------------------------------------------
# Format: redis://[[username]:[password]@]host[:port][/database]
# For local development: redis://localhost:6379/0
# For Railway: Use the REDIS_URL provided by Railway
REDIS_URL=redis://localhost:6379/0

# -----------------------------------------------------------------------------
# OpenAI / LLM Configuration (for AI agents)
# -----------------------------------------------------------------------------
# The API key for OpenAI (or compatible provider)
OPENAI_API_KEY=sk-your-openai-api-key

# Optional: Override the model used by agents (default: gpt-4)
# OPENAI_MODEL=gpt-4-turbo

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# Environment: development, staging, or production
ENVIRONMENT=development

# Enable debug mode (more verbose logging, auto-reload)
DEBUG=true

# The host and port for the API server
API_HOST=0.0.0.0
API_PORT=8000

# -----------------------------------------------------------------------------
# Celery Worker Settings
# -----------------------------------------------------------------------------
# Number of concurrent worker processes (default: 4)
CELERY_CONCURRENCY=4

# -----------------------------------------------------------------------------
# Security
# -----------------------------------------------------------------------------
# Secret key for signing tokens/sessions (generate with: openssl rand -hex 32)
SECRET_KEY=your-secret-key-generate-with-openssl-rand-hex-32

# Allowed origins for CORS (comma-separated)
# Example: http://localhost:3000,https://myapp.com
CORS_ORIGINS=http://localhost:3000
